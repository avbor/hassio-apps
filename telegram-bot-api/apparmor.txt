#include <tunables/global>

profile "{96e39688_telegram-bot-api,local_telegram-bot-api,telegram-bot-api}" flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability dac_override,
  capability kill,
  capability net_bind_service,
  capability setuid,
  capability setgid,
  capability chown,
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # Network
  network inet stream,
  network inet6 stream,

  # S6-Overlay init system
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /sbin/** ix,
  /usr/sbin/** ix,
  /lib/** ix,
  /usr/lib/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,

  # Standard devices
  /dev/tty rw,
  /dev/null rw,
  /dev/zero rw,
  /dev/random r,
  /dev/urandom r,

  # Bashio (Home Assistant helper library)
  /usr/lib/bashio/** ix,

  # Run script
  /run.sh rix,

  # App data
  /data/** rw,
  /media/** rw,

  # Temporary files
  /tmp/** rwk,
  /var/tmp/** rwk,

  # System files (read-only)
  /etc/** r,
  /proc/** r,
  /sys/class/** r,
  /sys/devices/** r,

  # Supervisor communication
  /run/supervisor.sock rw,
  owner /data/.backup.lock rwk,
  /data/.snapshot/** rw,

  /usr/local/bin/telegram-bot-api cx -> telegram_api_server,

  profile telegram_api_server flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>

    /proc/[0-9]*/stat r,

    network inet stream,
    network inet6 stream,

    signal (receive) peer="{96e39688_telegram-bot-api,local_telegram-bot-api,telegram-bot-api}",

    /data/ r,
    /data/** rwk,
    /media/** rw,
    /usr/local/bin/telegram-bot-api r,
    /lib/** rm,
    /usr/lib/** rm,

    /etc/ssl/openssl.cnf r,
    /etc/ssl/certs/ r,
    /etc/ssl/certs/** r,
    /usr/share/ca-certificates/ r,
    /usr/share/ca-certificates/** r,
    /etc/resolv.conf r,
    /etc/hosts r,
    /dev/urandom r,
    /dev/null rw,
  }
}