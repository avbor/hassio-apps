#include <tunables/global>

profile 96e39688_telegram-bot-api flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/openssl>
  #include <abstractions/consoles>

  # S6-Overlay v3
  /init rmix,
  /run.sh rmix,
  /bin/sh rmix,
  /bin/bash rmix,
  /usr/bin/bash rmix,

  # s6
  /etc/s6-overlay/** r,
  /usr/lib/s6-overlay/** rmix,
  /etc/s6-linux-init/** rmix,
  /run/s6/** rmix,
  /run/s6-rc/** rmix,
  /run/s6-linux-init/** rmix,
  /run/s6/basedir/bin/** rmix,
  /package/admin/s6-overlay-*/** rmix,
  /usr/libexec/s6-overlay/** rmix,
  
  # HA
  /etc/cont-init.d/** rmix,
  /etc/services.d/** rmix,
  /etc/cont-finish.d/** rmix,
  /package/** rmix,
  /command/** rmix,

  # Capabilities and Signals
  capability kill,
  capability setuid,
  capability setgid,
  capability chown,
  capability dac_override,
  capability sys_chroot
  capability dac_override
  
  signal (send, receive) set=(kill,term,int,hup,cont),
  signal (receive) peer=unconfined,

  # Network
  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  # System files and libs
  /bin/** rmix,
  /usr/bin/** rmix,
  /usr/local/bin/** rmix,
  /lib/** rm,
  /usr/lib/** rm,
  /usr/lib/bashio/** rmix,
  
  /etc/passwd r,
  /etc/group r,
  /etc/nsswitch.conf r,
  /etc/resolv.conf r,
  /etc/hosts r,
  /etc/ssl/certs/ca-certificates.crt r,
  
  /run/{,**} rwk,
  /tmp/** rwk,
  /dev/tty rw,
  /dev/null rw,
  /dev/urandom r,

  # App data
  /data/{,**} rw,
  /media/{,**} rw,

  /usr/local/bin/telegram-bot-api cx -> telegram_server,

  profile telegram_server flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>
    #include <abstractions/nameservice>
    #include <abstractions/openssl>

    network inet stream,
    network inet6 stream,

    signal (receive) peer="96e39688_telegram-bot-api",

    /data/{,**} rw,
    /media/{,**} rw,
    /usr/local/bin/telegram-bot-api r,
    /lib/** rm,
    /usr/lib/** rm,
    
    /etc/ssl/certs/ca-certificates.crt r,
    /etc/resolv.conf r,
    /etc/hosts r,
    /dev/urandom r,
    /dev/null rw,
  }
}